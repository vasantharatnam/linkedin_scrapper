export const LINKEDIN_SKILLS_LIMITATION =
  "Skills are returned only when the configured authorized LinkedIn internal endpoint exposes them. If that endpoint is unavailable, unconfigured, or denied by LinkedIn visibility controls, the API returns an empty skills array.";
